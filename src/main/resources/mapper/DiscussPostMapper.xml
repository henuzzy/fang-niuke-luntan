<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yue.nklt.mapper.DiscussPostMapper">

    <sql id="selectFields">
        id, user_id, title, content, type, status, create_time, comment_count, score
    </sql>

    <select id="selectPosts" resultType="DiscussPost">
        select <include refid="selectFields"/>
        from discuss_post
        where status != 2
        <choose>
            <when test="orderMode == 'hot'">
                order by score desc, create_time desc
            </when>
            <otherwise>
                order by type desc, create_time desc
            </otherwise>
        </choose>
        limit #{offset}, #{limit}
    </select>

    <select id="selectPostCount" resultType="int">
        select count(id)
        from discuss_post
        where status != 2
    </select>

    <select id="selectPostById" resultType="DiscussPost">
        select <include refid="selectFields"/>
        from discuss_post
        where id = #{id}
    </select>

    <insert id="insertPost" parameterType="DiscussPost" keyProperty="id" useGeneratedKeys="true">
        insert into discuss_post (user_id, title, content, type, status, create_time, comment_count, score)
        values (#{userId}, #{title}, #{content}, #{type}, #{status}, #{createTime}, #{commentCount}, #{score})
    </insert>

    <update id="updateCommentCount">
        update discuss_post set comment_count = #{commentCount}
        where id = #{id}
    </update>

    <update id="updateType">
        update discuss_post set type = #{type}
        where id = #{id}
    </update>

    <update id="updateStatus">
        update discuss_post set status = #{status}
        where id = #{id}
    </update>

</mapper>

